{
  "state": "local",
  
  "local": {
    "stories_file": "stories.json"
  },

  "commands": {
    "typecheck": "npm run typecheck",
    "lint": "npm run lint",
    "test": "npm run test",
    "build": "npm run build"
  },
  
  "branch_prefix": "feat"
}
